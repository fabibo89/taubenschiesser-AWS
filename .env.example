# ============================================
# Taubenschiesser - Root Environment
# ============================================
# Kopieren zu .env: cp .env.example .env

# ============================================
# MQTT Broker (für Hardware Monitor)
# ============================================
# IP deines Mosquitto-Servers in der Wohnung
MQTT_BROKER_IP=192.168.1.100

# Beispiele:
# MQTT_BROKER_IP=192.168.1.100  # Dein Mosquitto-Server
# MQTT_BROKER_IP=localhost      # Lokal auf dem gleichen Host
# MQTT_BROKER_IP=10.0.0.5       # Anderes Netzwerk

# ============================================
# Hinweise:
# ============================================
# 1. Kopiere diese Datei: cp .env.example .env
# 2. Trage deine Mosquitto-IP ein
# 3. Starte Docker Compose: docker-compose up -d
# 4. Hardware Monitor verbindet sich automatisch mit deinem MQTT
